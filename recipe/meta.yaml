{% set name = "xtal2png" %}
{% set version = "0.6.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/xtal2png-{{ version }}.tar.gz
  sha256: 917e4e31d39c835f5275530ad6639103f7b8d70a266dcf3bc855f7bc7c5fd666

build:
  number: 0
  entry_points:
    - xtal2png = xtal2png.core:run
  script: {{ PYTHON }} -m pip install . -vv
  noarch: python

requirements:
  host:
    - pip
    - setuptools-scm
    - python >=3.6
  run:
    - numpy
    - pillow
    - pymatgen
    - python >=3.6
    - importlib-metadata
    - plotly
    - python-kaleido
test:
  imports:
    - xtal2png
    - xtal2png.utils
  commands:
    - pip check
    - xtal2png --help
  requires:
    - pip

about:
  home: https://github.com/sparks-baird/xtal2png/
  summary: Encode and decode crystal structures via portable networks graphics (PNG) files.
  dev_url: https://github.com/sparks-baird/xtal2png/
  doc_url: https://xtal2png.readthedocs.io/
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - sgbaird
