{% set name = "xtal2png" %}
{% set version = "0.5.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/xtal2png-{{ version }}.tar.gz
  sha256: bacac66926a886d7edc10ed9900a3d884311d18958d2b66d2432d804dbb49adb

build:
  number: 0
  entry_points:
    - xtal2png = xtal2png.core:run
  script: {{ PYTHON }} -m pip install . -vv
  noarch: python

requirements:
  host:
    - pip
    - setuptools-scm
    - python >=3.6
  run:
    - numpy
    - pillow
    - pymatgen
    - python >=3.6
    - importlib-metadata
    - plotly
    - python-kaleido
test:
  imports:
    - xtal2png
    - xtal2png.utils
  commands:
    - pip check
    - xtal2png --help
  requires:
    - pip

about:
  home: https://github.com/sparks-baird/xtal2png/
  summary: Encode and decode crystal structures via portable networks graphics (PNG) files.
  dev_url: https://github.com/sparks-baird/xtal2png/
  doc_url: https://xtal2png.readthedocs.io/
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - sgbaird
